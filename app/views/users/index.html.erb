<%= provide(:title, 'User Listing') %>
<h1>User Listing</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= logo_for user %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

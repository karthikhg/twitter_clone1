<a href="<%= user_path(current_user) %>">
  <%= logo_for current_user %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "My Profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.posts.count, "kTweets!") %>
</span>

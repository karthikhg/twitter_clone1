<%= form_for(@post) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose a kTweet" %>
  </div>
  <%= f.submit "kTweet It!", class: "btn btn-large btn-primary" %>
<% end %>
